export const getFeedbacks = async () => {
  // upvotes: number;
  // title: string;
  // description: string;
  // commentCount: number;
  // filterText: string;
  const response = [
    {
      upvotes: 112,
      title: 'Add a mobile app',
      description: 'Add a mobile app for iOS and Android',
      commentCount: 4,
      filterText: 'Enhancement',
    },
  ];
  return response;
};
